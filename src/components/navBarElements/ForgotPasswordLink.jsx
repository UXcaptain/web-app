import { NavLink } from "react-router";

const ForgotPasswordLink = () => {
    return (
    <NavLink to="/auth/recover-password">recover password</NavLink>
    );
}

export default ForgotPasswordLink;